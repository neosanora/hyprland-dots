table inet filter {
    chain input {
        type filter hook input priority 0;
        policy drop;

        iif "lo" accept;
        ct state established,related accept;

        # Port default Tor OR connection control
        tcp dport { 9050, 9051 } accept;
    }
}
